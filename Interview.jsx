function Interview() {


    * anagrams('save', 'vase') === true
    * anagrams('C A T', 'act') === true
    * anagrams('frontend', 'simplified') === false
    */
   anagrams('frontend', 'simplified')
   
   const anagrams = (strA, strB) => {
       const resultA = strA.sort().toUpperCase().slice(" ")
       console.log(strA)
     
   }
    return (

    )
}

export default Interview()